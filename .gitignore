# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local raw data (not versioned)
data/raw/

# Python build artifacts
__pycache__/
*.pyc

# Transient CI artifact from scripts/check_stale_sources.py (never committed)
.stale-alert.txt

# Claude Code machine-local state. Note the `*.local` pattern above does NOT
# match `settings.local.json` — gitignore globs the whole filename, and that
# one ends in `.json`, which is why it was tracked for months.
.claude/
